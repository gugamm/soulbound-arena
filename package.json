{
  "name": "soulbound-arena",
  "version": "0.1.0",
  "type": "module",
  "description": "2D roguelite co-op action game for up to 4 players",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --import ./test/setup.mjs --test test/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "socket.io": "^4.7.5"
  }
}
